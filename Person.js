/**
 * Created by yidon on 03/07/2017.
 */
/*class Person{
    constructor(name, age){
        this.name = name;
        this.age = age;
    }
}

module.exports=Person;
 */
/*
const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const PersonSchema = Schema({
    first_name:{
        type : String,
        default : 'unknown',
        required : true
    },

    last_name:{
        type:String,
        default: 'unknown',
        required :true
    },

    email:{
        type:string,
        required: true
    },

    password:{
        type: String,
        required: true
    },

    birthday:{
        type: Date,
    }
});

module.exports = mongoose.model('Person', PersonSchema);
    */